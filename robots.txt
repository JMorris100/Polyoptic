User-agent: *
Allow: /

# A working page, not a published one. It also carries a noindex meta tag —
# this line only saves the crawl, the tag is what does the work.
Disallow: /board.html

Sitemap: https://polyoptic.co/sitemap.xml
